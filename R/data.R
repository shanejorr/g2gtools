#' Teacher pre-training survey.
#'
#' A dataset containing example responses to the G2G teacher pre-training survey.
#'
#' @format A data frame with 17 rows and 33 variables. Each row contains all of a teacher's responses
#'      and is in the same format as Google Sheets creates for Google Forms.
"teacher_pre_survey"

#' Classroom observation pre and psot training.
#'
#' A dataset containing example responses math classroom observations pre and post training
#'
#' @format A data frame with 14 rows and 32 variables. Each row contains a teacher's observation
#'      and is in the same format as Google Sheets creates for Google Forms.
"classroom_observations_math"
